# Skills Bundle 1 — store-asset slot

Drop the App Store / Play Store marketing artwork for the
**Companion Skills Bundle 1** in-app purchase here so the seed
script and submission checklist can find them by stable filename.

Required files (PNG, sRGB, no transparency):

| File                          | Size       | Purpose                                    |
|-------------------------------|------------|--------------------------------------------|
| `bundle-promo.png`            | 1024×1024  | Bundle SKU promo art (App Store IAP)       |
| `mem-says-icon.png`           | 1024×1024  | Per-skill IAP art — Mem Says               |
| `signal-sort-icon.png`        | 1024×1024  | Per-skill IAP art — Signal Sort            |
| `pattern-path-icon.png`       | 1024×1024  | Per-skill IAP art — Pattern Path           |
| `echo-count-icon.png`         | 1024×1024  | Per-skill IAP art — Echo Count             |
| `screenshot-skills-home.png`  | 1290×2796  | iPhone 6.7" screenshot of `/skills` home   |
| `screenshot-paywall.png`      | 1290×2796  | iPhone 6.7" screenshot of bundle paywall   |

Notes:

- The bundle SKU is `com.oc2co.memtool.skills.bundle1` ($2.99).
- Per-skill SKUs are `com.oc2co.memtool.skill.<id>` ($0.99 each).
- Pro does NOT auto-grant Skills — keep marketing copy clear that
  this is a separate one-time purchase.
- Filenames are referenced verbatim in the App Store Connect
  upload script; do not rename without updating that script.
